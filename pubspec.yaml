name: untitled
description: "BLE Chat Application - Flutter"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # BLE Central (Client) functionality
  flutter_blue_plus: ^1.35.2
  
  # Runtime permission handling
  permission_handler: ^11.3.1
  
  # State management
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
